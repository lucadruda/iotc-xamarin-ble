<?xml version="1.0" encoding="UTF-8"?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms" 
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
            xmlns:local="clr-namespace:iotc_xamarin_ble.Views"
            xmlns:android="clr-namespace:Xamarin.Forms.PlatformConfiguration.AndroidSpecific;assembly=Xamarin.Forms.Core"
            x:Class="iotc_xamarin_ble.Views.MainPage"
            android:TabbedPage.ToolbarPlacement="Bottom"
            BarBackgroundColor="Accent"
            BarTextColor="White" 
            ItemsSource="{Binding Pages}"
            SelectedItem="{Binding SelectedTabPage, Mode=TwoWay}">
    <TabbedPage.ItemTemplate>
        <DataTemplate>
            <NavigationPage Title="{Binding Title}" IconImageSource="{Binding Icon}">
                <x:Arguments>
                    <ContentPage Content="{Binding Content}"/>
                </x:Arguments>
            </NavigationPage>
        </DataTemplate>
    </TabbedPage.ItemTemplate>
</TabbedPage>