<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls1="clr-namespace:iotc_xamarin_ble.Graphics"
             x:Class="iotc_xamarin_ble.Views.ListPage" Title="{Binding Title}">
    <ContentPage.Content>
        <AbsoluteLayout VerticalOptions="Fill">
            <controls1:LoadingDialog/>
            <ListView x:Name="rootLayout" x:FieldModifier="public" ItemsSource="{Binding Items}" SelectedItem="{Binding LastTappedItem, Mode=TwoWay}" AbsoluteLayout.LayoutFlags="SizeProportional" AbsoluteLayout.LayoutBounds="20,20,1,1" SeparatorColor="Gray" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Name}" TextColor="Black"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>